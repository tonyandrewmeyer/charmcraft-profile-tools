name: Charm checks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_call:
  workflow_dispatch:

jobs:
  charm-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78  # v7.6.0
      - name: Install tox and tox-uv
        run: uv tool install tox --with tox-uv
      - name: Check kubernetes
        run: |
          cd kubernetes
          tox -e lint,unit
      - name: Implement kubernetes-extra
        run: |
          uvx --from rust-just just kubernetes-extra
          # Check that the implemented charm matches the one stored in the repo.
          # We don't care about differences in versions of transitive deps.
          git add --all
          git diff --exit-code HEAD -- . ':(exclude)kubernetes-extra/uv.lock'
      - name: Check kubernetes-extra
        run: |
          cd kubernetes-extra
          tox -e lint,unit
      - name: Check machine
        run: |
          cd machine
          tox -e lint,unit
